{
  "name": "ai-forum-web",
  "version": "0.0.0",
  "private": true,
  "description": "Front-end JS for the AI Forum. Pure ESM, no runtime deps; node:test for unit tests.",
  "type": "module",
  "engines": {
    "node": ">=21"
  },
  "scripts": {
    "test": "node --test \"src/test/js/**/*.test.mjs\"",
    "test:mcp": "node --test \"mcp/gh-readonly/test/**/*.test.mjs\""
  }
}
